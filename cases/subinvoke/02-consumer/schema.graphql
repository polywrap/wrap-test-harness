#import * into ImportedInvoke from "authority/imported-invoke"

type Module {
  addAndIncrement(a: Int!, b: Int!): Int!
  throwError(error: String!): Boolean!
  rethrowError(error: String!): Boolean!
}
