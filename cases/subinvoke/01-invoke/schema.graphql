#import * into ImportedSubinvoke from "ens/imported-subinvoke.eth"

type Module {
  addAndIncrement(a: Int!, b: Int!): Int!
  addFromPluginAndIncrement(a: Int!, b: Int!): Int!
  invokeThrowError(a: String!): Boolean!
}
