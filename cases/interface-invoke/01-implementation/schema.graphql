#import { Module, InterfaceType } into Interface from "wrap://authority/interface"

type Module implements Interface_Module {
  moduleMethod(
    arg: ImplementationType!
  ): ImplementationType!
}

type ImplementationType implements Interface_InterfaceType {
  str: String!
}